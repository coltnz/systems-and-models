# Systems & Models — environment template. Copy to `.env` (gitignored) for live runs.
# OPENAI_MODEL has NO default; the alpha demo + all tests run offline on the mock
# extraction adapter, so leaving these blank is fine until you wire bd-6's OpenAI adapter.
OPENAI_API_KEY=
OPENAI_MODEL=
# OPENAI_BASE_URL=
