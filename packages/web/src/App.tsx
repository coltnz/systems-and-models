import { SCHEMA_VERSION } from '@sam/types'

export default function App() {
  return (
    <main>
      <h1>Systems &amp; Models — review UI (scaffold)</h1>
      <p>
        Placeholder page. The reviewed-learning-graph review UI is rebuilt here
        as <code>@sam/web</code> (D-003). Schema contract version:{' '}
        <code>{SCHEMA_VERSION}</code>.
      </p>
    </main>
  )
}
